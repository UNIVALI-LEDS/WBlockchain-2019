pragma solidity ^0.4.2;

contract DualLoop {

  function execute(uint32 amount) public pure returns(uint32 size) {
    uint32 k;
    uint32 j;
    for (j = 0; j < amount; j++) {
      for (k = 0; k < amount; k++) {

      }
    }
    return amount;
  }
}
